// TODO: Rewrite these as arrow functions:

// 1.
function square(x) {
return x \* x;
}

// 2.
function isEven(n) {
return n % 2 === 0;
}

// 3.
function greet(name) {
return `Hello, ${name}!`;
}

// 4.
function calculate(a, b) {
const sum = a + b;
const product = a \* b;
return { sum, product };
}
