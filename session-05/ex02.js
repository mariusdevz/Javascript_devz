const square = (x) => x * x;

const isEven = (n) => n % 2 === 0;

const greet = (name) => {
    return `Hello, ${name}`;
}

const calculate = (a, b) => {
    const sum = a + b;
    const product = a * b;
    return { sum, product }
}

// console.log(calculate(5, 2));
