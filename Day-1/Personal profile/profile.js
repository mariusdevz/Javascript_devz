
const name = 'marius devz';
const age = 25;
const retiringAge = 65;
const city = 'Yaounde';
const jobTitle = 'Junior Developer';

const retirement = `${retiringAge - age}`;

console.log(`
    My Personal Profile:
    Name: ${name}
    Age: ${age}
    City: ${city}
    Job: ${jobTitle}
    Years until retirement: ${retirement}
    statement: I'm ${name}, I'm ${age} and live in ${city}
    `);


