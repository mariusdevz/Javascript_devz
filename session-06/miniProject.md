### Mini Project 6.1: Weather App (API Calling)

```javascript
// Fetch weather data from a free API

// Create a function getWeather(city) that:
// 1. Uses fetch to get weather from:
//    https://wttr.in/[city]?format=j1
// 2. Parses the JSON response
// 3. Extracts: temperature, condition, humidity
// 4. Returns an object with the weather data
//
// Create a function displayWeather(city) that:
// 1. Calls getWeather(city)
// 2. Logs the weather nicely formatted
// 3. Handles errors gracefully
//
// Test with several cities
```
